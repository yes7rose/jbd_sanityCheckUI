#
# Copyright (c) 2012 Shotgun Software, Inc
# ----------------------------------------------------
#
# Metadata defining the behaviour and requirements for this app

# expected fields in the configuration file for this app
configuration:
    display_name:
        type:           str
        default_value:  createShotCam
        description:    Fetches the latest layout camera for the current shot

    fps:
        type:           int
        default_value:  25
        description:    Frames per second, default PAL 25fps, valid options are 24, 25, 30

    gateImagePath:
        type:           str
        default_value:  pathtofile
        description:    path to the gate image

    suffix:
        type:           str
        default_value:  shotCam
        description:    the camera suffix this assumes a _ will preceed it eg sh010_shotCam

# the Shotgun fields that this app needs in order to operate correctly
requires_shotgun_fields:

# More verbose description of this item 
display_name: "Camera..."
description: ""

# Required minimum versions for this item to run
requires_shotgun_version:
requires_core_version: "v0.14.x"
requires_engine_version: "v0.4.x"

# For now this app is only compatible with Nuke.
supported_engines: ["tk-maya"]

# the frameworks required to run this app
frameworks:
    - {"name": "tk-framework-shotgunutils", "version": "v2.x.x"}
